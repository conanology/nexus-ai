# ============================================================================
# Nexus AI — Local Mode Configuration
# ============================================================================
# Copy this file to .env.local and fill in the required values.
# Get your API key from: https://aistudio.google.com/apikey
# ============================================================================

# === LOCAL MODE (activates automatically when NEXUS_BUCKET_NAME is not set) ===
STORAGE_MODE=local
LOCAL_STORAGE_PATH=./local-storage

# === AI SERVICES (required) ===
# Google AI Studio API key — used for:
#   - Script generation (Gemini LLM)
#   - Director Agent (scene classification)
#   - Image generation (Gemini)
#   - TTS (AI Studio TTS)
NEXUS_GEMINI_API_KEY=

# === OPTIONAL SERVICES ===
# Giphy API key for meme reaction GIFs (replaces Tenor, which was discontinued Jan 2026)
# Get a free key at: https://developers.giphy.com/
# GIPHY_API_KEY=

# Pexels API key for stock photos/videos as scene backgrounds
# Free tier: 200 requests/hour. Get a key at: https://www.pexels.com/api/
# PEXELS_API_KEY=

# === NOT NEEDED IN LOCAL MODE ===
# These are only needed for cloud deployment — leave unset for local mode
# NEXUS_BUCKET_NAME=              # Not needed — using local filesystem
# NEXUS_PROJECT_ID=               # Not needed — no GCP services
# GOOGLE_APPLICATION_CREDENTIALS= # Not needed — using API key auth
# GOOGLE_CLOUD_PROJECT=           # Not needed — no GCP services
# NEXUS_SECRET=                   # Not needed — no render service auth
# RENDER_SERVICE_URL=             # Not needed — rendering locally
# NEXUS_DISCORD_WEBHOOK_URL=      # Not needed — no notifications
